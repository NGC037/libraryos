services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: libraryos
      POSTGRES_PASSWORD: neha398
      POSTGRES_DB: libraryos
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
